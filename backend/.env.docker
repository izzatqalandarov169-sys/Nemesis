# ========================================
# .env.docker - Configuration for Docker deployment
# ========================================
# This file is an EXAMPLE for Docker-based deployment.
# Copy this file to .env and adjust values as needed.
#
# KEY DIFFERENCES from non-Docker .env:
# - POSTGRES_HOST: Use 'postgres' (Docker service name) instead of 'localhost'
# - BACKEND_API_URL: Keep as localhost for MCP (which runs on host, not in Docker)
# ========================================

# ========================================
# APPLICATION METADATA
# ========================================
PROJECT_NAME=NEMESIS
PROJECT_TAGLINE=AI-Driven Security Audit Platform
PROJECT_DESCRIPTION=Intelligent autonomous penetration testing powered by AI
VERSION=1.1.0
API_V1_PREFIX=/api

# ========================================
# DATABASE CREDENTIALS
# IMPORTANT: Change these values in production!
# ========================================
POSTGRES_USER=nemesis
POSTGRES_PASSWORD=nemesis
POSTGRES_DB=nemesis_assessments
# IMPORTANT: Use 'postgres' (Docker service name) instead of 'localhost'
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# DATABASE_URL is intentionally omitted here â€” config.py constructs it
# automatically from the POSTGRES_* variables above. Hardcoding it here
# caused drift bugs when the DB name changed.

# ========================================
# CORS ORIGINS
# Comma-separated list. Wildcard "*" is rejected at startup because it is
# incompatible with allow_credentials=True per the CORS spec.
# ========================================
BACKEND_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# ========================================
# CONTAINER CONFIGURATION
# Default: nemesis-pentest (built-in). Set to your Exegol container name if using Exegol.
# ========================================
CONTAINER_WORKSPACE_BASE=/workspace
DEFAULT_CONTAINER_NAME=nemesis-pentest
COMMAND_TIMEOUT=300

# ========================================
# LOGGING CONFIGURATION
# ========================================
LOG_LEVEL=INFO
LOG_FORMAT=json
LOG_DIR=logs
LOG_FILE_ENABLED=true
LOG_CONSOLE_ENABLED=true
LOG_FILE_MAX_BYTES=10485760
LOG_FILE_BACKUP_COUNT=5

# ========================================
# BACKEND API URL (for MCP server)
# IMPORTANT: Keep as localhost:8000 because MCP runs on HOST, not in Docker
# ========================================
BACKEND_API_URL=http://localhost:8000/api

# ========================================
# DEVELOPMENT / PRODUCTION
# ========================================
ENVIRONMENT=production
DEBUG=false
